apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: rendered-dev
  namespace: flux-system
spec:
  interval: 1m
  url: ssh://git@github.com/skiyl9x/portfolio
  ref:
    branch: dev      # <- dev branch з rendered manifests
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: rendered-stage
  namespace: flux-system
spec:
  interval: 1m
  url: ssh://git@github.com/skiyl9x/portfolio
  ref:
    branch: stage      # <- stage branch з rendered manifests

