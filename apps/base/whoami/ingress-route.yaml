apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: whoami
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`whoami.minikube.cluster`)
      kind: Rule
      services:
        - name: whoami
          port: 80
